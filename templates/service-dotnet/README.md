# Dotnet Service Template

Template contract:
- .NET 10 Web API
- `/health` endpoint
- Swagger UI and OpenAPI JSON
- Roslyn analyzers enabled by default
- `dotnet format` compatible
- container build target

Scaffold starter assets live under `scaffold/`.
