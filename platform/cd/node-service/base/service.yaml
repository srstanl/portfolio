apiVersion: v1
kind: Service
metadata:
  name: node-service
  labels:
    app: node-service
spec:
  selector:
    app: node-service
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
