# Node Service Template

Template contract:
- Fastify service
- `/health` endpoint
- Swagger UI (`/docs`) and OpenAPI JSON (`/docs/json`)
- ESLint defaults
- Node test runner defaults
- container build target

Scaffold starter assets live under `scaffold/`.
